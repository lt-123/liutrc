#!/usr/bin/env bash

eval "$(starship init zsh)"
eval "$(zoxide init zsh)"

alias cat=bat
alias top=btm
alias htop=btm
alias du=dust
alias ls=lsd
alias tree=lsd --tree
alias find=fd
alias grep=rg
alias man=tldr

